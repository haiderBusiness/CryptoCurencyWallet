import { Sizes } from "../constants/Sizes";

export default function useThemeSizes() {
  //TODO:
  const deviceDimentions = "test";

  if (deviceDimentions === "test") {
    return Sizes.iphoneXsMax;
  } else {
    return Sizes.iphoneXsMax;
  }
}
